根据以下 HTTP 接口规格，判断**请求或响应**中可能涉及的个人信息或敏感数据类型（仅依据已给出的字段名、类型、描述、路径等，不要臆测未出现的信息）。

允许输出的 `types` 元素**必须且仅能**从下列英文代码中选取（可多选；若无则输出空数组 []）：

{{allowedTypes}}

接口规格（JSON 文本）：

{{toolSpec}}

**输出要求**：只输出**一个** JSON 对象，不要 Markdown、不要代码围栏、不要任何解释性文字。格式严格为：

{"types":["CODE",...],"summary":"一两句中文说明判断依据或为何为空"}
