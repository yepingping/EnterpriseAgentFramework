apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-skills-service
  labels:
    app: ai-skills-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-skills-service
  template:
    metadata:
      labels:
        app: ai-skills-service
    spec:
      containers:
        - name: ai-skills-service
          image: registry.enterprise.com/ai/ai-skills-service:latest
          ports:
            - containerPort: 8602
          env:
            - name: MYSQL_URL
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: mysql-url
            - name: MYSQL_USER
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: mysql-user
            - name: MYSQL_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: ai-secrets
                  key: mysql-password
            - name: REDIS_HOST
              value: redis
            - name: MILVUS_HOST
              value: milvus
            - name: MODEL_SERVICE_URL
              value: http://ai-model-service:8601
          resources:
            requests:
              memory: "512Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1000m"
          livenessProbe:
            httpGet:
              path: /ai/actuator/health
              port: 8602
            initialDelaySeconds: 30
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: ai-skills-service
spec:
  selector:
    app: ai-skills-service
  ports:
    - port: 8602
      targetPort: 8602
  type: ClusterIP
